# Интерфейс зав. склада + amoCRM

Веб-инструмент для заведующего складом. Работает поверх amoCRM: диспетчер
переводит сделку на этап «Отгрузка» → создаётся связанная сделка в воронке
«Склад» → зав. склада заполняет факт, единицу и закупочную цену отдельно для
каждого выбранного материала, общие поддоны и доставку, прикладывает любое число
накладных → данные и файлы возвращаются в исходную сделку диспетчера → она
переходит на «Получение накладной», а складская сделка — на «Готово».

## Стек

- Next.js 16 (App Router), TypeScript, Tailwind
- amoCRM API v4 (долгосрочный токен, хранится только в env на сервере)
- amoCRM Files API (частичная загрузка файлов без ограничения их количества)
- SQLite (`better-sqlite3`) для черновиков и построчных складских данных
- `decimal.js` для точного расчёта количества и денежных сумм

Текущая SQLite-версия рассчитана на один постоянный локальный сервер. Перед
переносом на Vercel или несколько экземпляров приложения базу нужно перенести в
PostgreSQL.

## Запуск локально

```bash
npm install
cp .env.local.example .env.local   # заполнить значения
npm run discover                    # вывести ID воронок / этапов / полей
# перенести нужные ID в .env.local
npm run dev
```

Открыть `http://localhost:3000/s/<токен-из-WAREHOUSE_USERS>`.

Проверки перед запуском:

```bash
npm test
npm run lint
npx tsc --noEmit
npm run build
```

## Переменные окружения

См. `.env.local.example`. Обязательные для старта: `AMOCRM_SUBDOMAIN`,
`AMOCRM_ACCESS_TOKEN`. Остальные ID заполняются после `npm run discover`.

`WAREHOUSE_DB_PATH` задаёт путь к SQLite-файлу. По умолчанию используется
`data/warehouse.sqlite`; каталог создаётся автоматически и исключён из Git.

Для загрузки накладных у интеграции amoCRM должен быть включён scope
**«Доступ к файлам»**. Scope удаления файлов не требуется.

## Вебхук amoCRM

В настройках amoCRM добавить вебхук на событие **«Смена этапа сделки»**:

```
URL: https://<домен>/api/amo-webhook?secret=<AMOCRM_WEBHOOK_SECRET>
```

Приложение реагирует только на переход в воронке диспетчера
(`AMOCRM_DISPATCH_PIPELINE_ID`) на этап `AMOCRM_DISPATCH_SHIPPING_STATUS_ID`.
После завершения складом исходная сделка переводится на этап
`AMOCRM_DISPATCH_WAYBILL_STATUS_ID`.

## Структура

```
src/lib/amocrm.ts     — клиент amoCRM API
src/lib/env.ts        — переменные окружения
src/lib/auth.ts       — именные ссылки зав. склада
src/lib/requests.ts   — доменный слой (сделка ⇄ заявка на отгрузку)
src/lib/warehouse-db.ts — SQLite, миграции, версии и черновики
src/lib/warehouse-calculations.ts — точные построчные и итоговые расчёты
src/app/api/amo-webhook/route.ts   — приём вебхука
src/app/api/requests/                — JSON API (список + заявка), доступ по токену
src/app/api/requests/[id]/files/     — список и частичная загрузка накладных
src/app/s/[token]/                 — интерфейс зав. склада
scripts/discover.mjs      — вывод ID воронок/этапов/полей из amoCRM
scripts/create-fields.mjs — создание кастомных полей склада (идемпотентно)
```

## Дедупликация

После создания складской сделки сделка диспетчера помечается тегом
`AMOCRM_PROCESSED_TAG`. Повторный вебхук по той же сделке ничего не создаёт.
ID исходной сделки пишется в поле `AMOCRM_FIELD_SOURCE_LEAD` складской сделки.

## Несколько материалов

Все значения поля `Материал / Номенклатура` читаются как отдельные позиции.
Черновик не меняет amoCRM. При завершении общие поддоны, доставка, итог и файлы
передаются диспетчеру. Для одной позиции также заполняются существующие скалярные
поля факта и цены; для нескольких позиций эти два поля намеренно не заполняются,
поскольку построчные значения хранятся в SQLite.

## Что ещё не сделано

- Уведомления зав. складу о новой заявке (Wazzup)
- Отчёт «Табель отгрузки» (Excel, прибыль по каждой отгрузке)
- Поддержка нескольких рейсов в одной заявке
